import React from 'react'

const ContactHeader = () => {
  return (
    <div className='contact_section
    '>
        <h1>
            CONTACT US
        </h1>
        <p> The camelCasing convention, used only for parameter names, <br/>capitalizes the first character of each word except the first word, as shown in the following examples. As the example also shows, <br/>two-letter acronyms that begin a camel-cased identifier are both lowercase.</p>

    </div>
  )
}

export default ContactHeader